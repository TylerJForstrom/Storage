[package]
name = "glassdb-wasm"
version = "0.1.0"
edition = "2021"
description = "WebAssembly bindings for GlassDB — runs the real engine in the browser."
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
glassdb = { path = "../glassdb" }
wasm-bindgen = "0.2"
